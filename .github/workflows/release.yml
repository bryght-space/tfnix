name: Publish

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:

  publish-github:
    name: Publish to release branch
    runs-on: ubuntu-latest

    steps:
    - name: Run release
      run: "sh/genRelease.sh"
      shell: bash
